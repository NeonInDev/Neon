Neon - Estado atual

AVISO: Esta cópia da Neon NÃO tem chave(s) de API configurada(s). Sem as chaves (por exemplo, GROQ_API_KEY, DEEPSEEK_API_KEY, TOKEN), o bot NÃO conseguirá responder a mensagens ou usar provedores de IA.

O que foi feito:
- Arquivo criado para registrar o estado atual antes do deploy/push.

Como resolver localmente:
1. Adicionar as chaves no arquivo .env ou variáveis de ambiente.
2. Reiniciar o serviço.

Observação: O arquivo .env não será commitado por padrão — confirmar que não há segredos no repositório antes do push.